name: ariza_takip
description: Arıza Takip uygulaması - full
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.5
  intl: ^0.18.0
  dropdown_search: ^5.0.3
  flutter_barcode_scanner: ^2.0.0
  firebase_core: ^2.10.0
  cloud_firestore: ^4.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
